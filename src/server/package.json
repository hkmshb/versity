{
  "name": "versity-api",
  "version": "0.1.0",
  "private": true,
  "description": "Versity API server",
  "main": "src/index.ts",
  "scripts": {
    "build": "rm -rf ./build && tsc",
    "watch": "tsc --watch",
    "start": "nodemon --watch . --exec ts-node ."
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^12.6.8",
    "nodemon": "^1.19.1",
    "ts-node": "^8.3.0",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  }
}
